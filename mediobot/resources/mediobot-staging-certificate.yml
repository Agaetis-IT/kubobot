apiVersion: certmanager.k8s.io/v1alpha1
kind: Certificate
metadata:
  name: mediobot-certificate-staging
spec:
  secretName: mediobot-certificate-staging-tls
  issuerRef:
    name: letsencrypt-staging-cluster-issuer
    kind: ClusterIssuer
  commonName: mediobot.monobot.fr
  acme:
    config:
    - http01: {}
      domains:
      - mediobot.monobot.fr